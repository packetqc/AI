# Core ML framework — install with CUDA support (choose your CUDA version):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Or CPU-only:
#   pip install torch
torch>=2.0.0

# HuggingFace stack: Qwen2 model + tokenizer + weight serialisation
transformers>=4.40.0
tokenizers>=0.19.0
safetensors>=0.4.0
huggingface_hub>=0.23.0

# GGUF file format (write + read — required for Ollama export)
gguf>=0.6.0

# Numerical ops used by the training pipeline
numpy>=1.24.0

# NPU export: ONNX graph format + INT8 quantisation + dynamo op translation
onnx>=1.15.0
onnxruntime>=1.17.0
onnxscript>=0.1.0
torchvision>=0.15.0
onnx-simplifier>=0.4.0

# ── Blackbox model security RE (model_security_re.py) ───────────────────────────
# YARA rule matching over the raw model blob (binary_forensics.md).
yara-python>=4.5.0
# Composition-graph modelling for grammar reconstruction.
networkx>=3.0
# Note: `strings` and `binwalk` are SYSTEM tools (not pip):
#   Debian/Kali:  sudo apt install binwalk binutils
# Optional (recommended) deeper binary forensics — Mandiant tools:
#   pip install flare-floss flare-capa
# Optional semantic-detection / cross-case memory layer (Phase 2):
#   pip install chromadb sentence-transformers mem0ai scikit-learn

# ── Optional ──────────────────────────────────────────────────────────────────

# PDF grammar extraction (model_tools_grammar.py --format pdf)
# pip install pypdf
# pypdf>=4.0.0
