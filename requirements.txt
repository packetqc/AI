# Core ML framework — install with CUDA support (choose your CUDA version):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Or CPU-only:
#   pip install torch
torch>=2.0.0

# HuggingFace stack: Qwen2 model + tokenizer + weight serialisation
transformers>=4.40.0
tokenizers>=0.19.0
safetensors>=0.4.0
huggingface_hub>=0.23.0

# GGUF file format (write + read — required for Ollama export)
gguf>=0.6.0

# Numerical ops used by the training pipeline
numpy>=1.24.0

# NPU export: ONNX graph format + INT8 quantisation for STM32Cube.AI
onnx>=1.15.0
onnxruntime>=1.17.0

# ── Optional ──────────────────────────────────────────────────────────────────

# PDF grammar extraction (model_tools_grammar.py --format pdf)
# pip install pypdf
# pypdf>=4.0.0

# ONNX dynamo export path (PyTorch >= 2.1 defaults to dynamo=True)
# Required only if dynamo=True is used in torch.onnx.export — not needed
# with the legacy TorchScript exporter (dynamo=False, current default here).
# pip install onnxscript
# onnxscript>=0.1.0
